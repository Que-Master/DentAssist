@model IEnumerable<DentAssist.Models.Entities.Odontologo>

@{
    ViewData["Title"] = "Gestion";
}

<h1>Gestion Odontologo</h1>

<p>
    <a asp-action="Create" class="btn btn-success">➕ Crear nuevo</a>
</p>

<table class="table table-bordered table-hover shadow-sm">
    <thead class="table-light">
        <tr>
            <th>@Html.DisplayNameFor(model => model.Nombre)</th>
            <th>@Html.DisplayNameFor(model => model.Matricula)</th>
            <th>@Html.DisplayNameFor(model => model.Especialidad)</th>
            <th>@Html.DisplayNameFor(model => model.Email)</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @if (!Model.Any())
        {
            <tr>
                <td colspan="5" class="text-center text-muted">No se encontraron odontólogos.</td>
            </tr>
        }
        else
        {
            foreach (var item in Model)
            {
                <tr>
                    <td>@item.Nombre</td>
                    <td>@item.Matricula</td>
                    <td>@item.Especialidad</td>
                    <td>@item.Email</td>
                    <td class="text-nowrap">
                        <a asp-action="Edit" asp-route-id="@item.IdOdontologo" class="btn btn-sm btn-warning me-1">
                            ✏️ Editar
                        </a>
                        <a asp-action="Details" asp-route-id="@item.IdOdontologo" class="btn btn-sm btn-info me-1 text-white">
                            🔍 Detalles
                        </a>
                        <a asp-action="Delete" asp-route-id="@item.IdOdontologo" class="btn btn-sm btn-danger">
                            🗑️ Eliminar
                        </a>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>

<div class="text-start">
    <a asp-controller="Administrador" asp-action="Menu" class="btn btn-outline-primary">
        ⬅️ Volver al Menú de Administrador
    </a>
</div>